{
  "id": "clawrouter",
  "name": "OpenClaw Router",
  "description": "Smart LLM router — 30+ models, API key authentication, 78% cost savings",
  "configSchema": {
    "type": "object",
    "properties": {
      "routing": {
        "type": "object",
        "description": "Override default routing configuration"
      }
    }
  }
}
