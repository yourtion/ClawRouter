{
  "version": "2.0",
  "providers": [
    {
      "id": "openrouter",
      "type": "openrouter",
      "enabled": true,
      "priority": 100,
      "auth": {
        "type": "api_key",
        "credentials": {
          "apiKey": "${OPENROUTER_API_KEY}"
        }
      },
      "models": {
        "autoSync": true
      }
    },
    {
      "id": "nvidia",
      "type": "nvidia",
      "enabled": true,
      "priority": 90,
      "auth": {
        "type": "api_key",
        "credentials": {
          "apiKey": "${NVIDIA_API_KEY}"
        }
      },
      "models": {
        "autoSync": true
      }
    }
  ]
}
